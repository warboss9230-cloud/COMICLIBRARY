// Dhruva Page JS

console.log("Dhruva page loaded");

// Example: Comic click alert (optional)

document.querySelectorAll(".comic-card").forEach(card => {
    card.addEventListener("click", () => {
        alert("Comic detail page coming soon!");
    });
});
